$(function(){

/*	var n = prompt ('n?', '');

	function sumTo (n) {
		var res = 0;
		for (var i=1; i<=n; i++) {
			res = res + i;
		}
		return res;
	}

	var fff = function sumTo (n) {
		if (n == 1) return 1;
		return n + sumTo(n-1);
	}

	alert(fff(n));*/
});